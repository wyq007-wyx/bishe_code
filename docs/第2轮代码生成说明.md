# 第2轮代码生成说明

## 1. 输入文档

- `docs/第1轮代码生成说明.md`
- `docs/models数据结构详解.md`
- `模块化代码实现计划.md`
- `完整实验方案.md`
- 当前已有代码（`tebs_scheduler`）

## 2. 本轮目标（按“代码生成总顺序”第2轮）

- 生成配置模块：`tebs_scheduler/src/tebs/config.py`
- 生成默认配置：`tebs_scheduler/configs/default_sim.yaml`
- 生成测试：`tebs_scheduler/tests/test_config.py`
- 对齐计划中的第2轮扩展文件：`configs/scenarios.yaml`、`configs/gurobi.yaml`
- 统一默认参数与单位约定：`Delta t=60s`、`H=12`、`TimeLimit=5s`、能量单位 `J`

## 3. 本轮生成结果

### 3.1 文件变更

新增文件：

- `tebs_scheduler/src/tebs/config.py`
- `tebs_scheduler/configs/default_sim.yaml`
- `tebs_scheduler/configs/scenarios.yaml`
- `tebs_scheduler/configs/gurobi.yaml`
- `tebs_scheduler/tests/test_config.py`
- `docs/第2轮代码生成说明.md`

更新文件：

- `tebs_scheduler/src/tebs/__init__.py`（导出配置对象与加载接口）
- `tebs_scheduler/pyproject.toml`（补充 `PyYAML>=6.0` 依赖）

### 3.2 `config.py` 实现内容

实现了第2轮要求的配置对象与加载逻辑：

- `SimulationConfig`
- `GurobiConfig`
- `ThermalConfig`
- `EnergyConfig`
- `ConfigBundle`
- `load_config(...)` / `load_config_from_mapping(...)`
- `default_config_path()`

#### 3.2.1 配置对象职责

- `SimulationConfig`：保存仿真层参数，包括 `delta_t_seconds`、`horizon_slots`、`simulation_slots`、异构核心数量、日照/阴影周期和能量单位。
- `GurobiConfig`：保存求解器参数，包括 `gurobi_time_limit`、`gurobi_mip_gap`、`gurobi_threads` 和 `gurobi_verbose`。
- `ThermalConfig`：保存热模型参数，包括初始温度、温度上限、环境温度、等效热容和冷却系数。
- `EnergyConfig`：保存能量模型参数，包括电池容量上限、最低安全电量、初始电量、基础功耗、太阳能峰值采集功率和轨道相位功率模型。
- `ConfigBundle`：把上述四类配置聚合在一起，作为后续模块统一读取的配置入口。

#### 3.2.2 配置加载流程

`load_config(config_path=None)` 是对外主入口：

1. 如果调用方未传入路径，则通过 `default_config_path()` 定位到 `tebs_scheduler/configs/default_sim.yaml`。
2. `_load_yaml_mapping(...)` 负责读取 YAML 文件，并检查根节点是否为 mapping。
3. `load_config_from_mapping(...)` 将 YAML 字典拆分为 `simulation`、`gurobi`、`thermal`、`energy` 四个 section。
4. 每个 section 缺失时使用空字典处理，每个字段缺失时回退到代码中的默认值。
5. 最终构造 `ConfigBundle`，后续模块只需要消费 `cfg.simulation`、`cfg.energy`、`cfg.thermal`、`cfg.gurobi`。

#### 3.2.3 字段读取与校验逻辑

配置模块使用一组私有读取函数保证输入类型清晰：

- `_read_int(...)`：读取整数，禁止把 `bool` 当作整数使用。
- `_read_float(...)`：读取数值，允许 `int` 或 `float`，但禁止 `bool`。
- `_read_str(...)`：读取非空字符串。
- `_read_bool(...)`：读取布尔值。

构造 dataclass 后还会执行范围校验：

- `delta_t_seconds`、`horizon_slots`、`simulation_slots` 必须大于 0。
- 高性能核数量和低功耗核数量必须非负，且总核心数必须大于 0。
- 日照时长和阴影时长必须非负，二者之和必须大于 0。
- `energy_unit` 必须为 `J`，确保内部能量计算单位统一。
- `gurobi_time_limit` 必须大于 0，`gurobi_mip_gap` 和线程数必须非负。
- `e_min_j < e_max_j`，且 `initial_energy_j` 必须落在 `[e_min_j, e_max_j]` 内。
- `solar_power_profile` 必须为 `constant` 或 `half_sine_orbit`。
- `solar_power_min_ratio` 必须落在 `[0, 1]` 内。
- `thermal_capacity_j_per_c` 必须大于 0，`cooling_coeff` 必须非负，`t_max_celsius` 必须高于环境温度。

#### 3.2.4 核心数量兼容逻辑

当前代码将核心数量拆成了两类：

- `high_performance_core_count`
- `low_power_core_count`

这是为了贴合实验方案中“2 个高性能核 + 2 个低功耗核”的异构多核设定。为了兼容旧配置，`_resolve_core_type_counts(...)` 仍支持旧字段 `core_count`：

- 若 YAML 中显式给出 `high_performance_core_count` 或 `low_power_core_count`，则优先使用新版字段。
- 若 YAML 中只给出旧版 `core_count`，则自动拆分为两类核心：高性能核为 `core_count // 2`，低功耗核为剩余数量。
- `SimulationConfig.core_count` 作为只读属性保留，返回两类核心数量之和，方便旧代码继续访问总核心数。

#### 3.2.5 异常设计

所有配置相关错误统一抛出 `ConfigError`，包括：

- 配置文件不存在；
- YAML 根节点不是字典结构；
- section 类型不是 mapping；
- 字段类型错误；
- 字段数值范围非法；
- 能量单位不是 `J`。

这种设计让后续 `task_factory.py`、`environment.py`、`energy_model.py`、`thermal_model.py` 和 Gurobi 相关模块可以在配置加载阶段尽早失败，避免错误延迟到仿真运行中才暴露。

### 3.3 默认参数落地

`configs/default_sim.yaml` 已固化关键默认参数：

- `simulation.delta_t_seconds = 60`
- `simulation.horizon_slots = 12`
- `simulation.simulation_slots = 194`
- `simulation.high_performance_core_count = 2`
- `simulation.low_power_core_count = 2`
- `simulation.sunlight_duration_slots = 61`
- `simulation.eclipse_duration_slots = 36`
- `gurobi.gurobi_time_limit = 5.0`
- `gurobi.gurobi_mip_gap = 0.01`
- `simulation.energy_unit = J`

并补充了后续模块直接可复用的初值：

- 能量边界与初始电量（`e_max_j = 144000.0` / `e_min_j = 115200.0` / `initial_energy_j = 129600.0`）
- 温度初值与上限（`initial_temperature_celsius = 25.0` / `t_max_celsius = 70.0`）
- 环境相关缺省功率（`default_base_power_w = 3.6` / `default_max_solar_power_w = 14.0`，其中太阳能功率为峰值）
- 太阳能轨道变化模型（`solar_power_profile = half_sine_orbit` / `solar_power_min_ratio = 0.0`）
- 近地轨道日照/阴影周期（约 `60 min 52 s` 日照、`35 min 38 s` 阴影，按 60 s 时隙取整为 `61/36 slots`）
- 详细依据见 `docs/默认参数设置依据.md`

### 3.4 第2轮扩展配置文件

为后续轮次预置：

- `configs/scenarios.yaml`：S1/S2/S3 场景模板
- `configs/gurobi.yaml`：`fast/balanced/quality` 求解参数模板

`configs/scenarios.yaml` 中三个场景的用途如下：

- `S1_light`：轻负载场景，任务数量较少，高功耗任务比例较低，适合后续基础冒烟测试。
- `S2_mixed`：混合负载默认对比场景，作为常规实验配置模板。
- `S3_stress`：高压力场景，高功耗任务比例更高，用于验证调度方法在资源紧张条件下的表现。

`configs/gurobi.yaml` 中三个 profile 的用途如下：

- `fast`：更短的 `TimeLimit` 和更宽松的 `MIPGap`，用于快速验证。
- `balanced`：与默认实验参数一致，用于主要对比实验。
- `quality`：更长的 `TimeLimit` 和更严格的 `MIPGap`，用于追求更高解质量的实验。

### 3.5 包导出与依赖更新

`tebs_scheduler/src/tebs/__init__.py` 已导出第2轮新增配置接口：

- `ConfigBundle`
- `ConfigError`
- `SimulationConfig`
- `GurobiConfig`
- `ThermalConfig`
- `EnergyConfig`
- `default_config_path`
- `load_config`
- `load_config_from_mapping`

这样后续代码既可以使用 `from tebs.config import load_config`，也可以使用 `from tebs import load_config`。

`pyproject.toml` 中补充了 `PyYAML>=6.0`，因为 `load_config(...)` 需要通过 `yaml.safe_load(...)` 解析 YAML 配置文件。

## 4. 测试与验收

执行命令：

```bash
cd tebs_scheduler
pytest
```

结果：

```text
16 passed
```

### 4.1 `tests/test_config.py` 测试内容

`test_round2_default_values()` 验证默认配置文件可被成功加载，并检查第2轮最关键的默认参数：

- `delta_t_seconds = 60`
- `horizon_slots = 12`
- `simulation_slots = 194`
- `high_performance_core_count = 2`
- `low_power_core_count = 2`
- `core_count = 4`
- `sunlight_duration_slots = 61`
- `eclipse_duration_slots = 36`
- `e_max_j = 144000.0`
- `e_min_j = 115200.0`
- `initial_energy_j = 129600.0`
- `default_base_power_w = 3.6`
- `default_max_solar_power_w = 14.0`
- `solar_power_profile = half_sine_orbit`
- `solar_power_min_ratio = 0.0`
- `initial_temperature_celsius = 25.0`
- `t_max_celsius = 70.0`
- `thermal_capacity_j_per_c = 3000.0`
- `gurobi_time_limit = 5.0`
- `energy_unit = J`

`test_missing_sections_fallback_to_reasonable_defaults(...)` 构造一个只包含 `simulation.simulation_slots` 的临时 YAML 文件，验证：

- 显式配置的 `simulation_slots` 会被读取；
- 未配置的 `delta_t_seconds` 会回退到 `60`；
- 未配置的 `gurobi_time_limit` 会回退到 `5.0`；
- 未配置的 `energy.e_min_j` 会回退到 `115200.0`；
- 未配置的 `energy.default_max_solar_power_w` 会回退到 `14.0`。

`test_missing_file_raises_clear_error(...)` 使用一个不存在的路径调用 `load_config(...)`，验证会抛出 `ConfigError`，且错误信息包含 `Configuration file not found`。

`test_invalid_values_raise_clear_error(...)` 构造 `delta_t_seconds = -1` 的非法配置，验证会抛出 `ConfigError`，并能定位到 `delta_t_seconds must be > 0`。

`test_core_count_config_supports_new_and_legacy_keys(...)` 同时验证新版和旧版核心数量配置：

- 新版配置 `high_performance_core_count = 3`、`low_power_core_count = 1` 时，`core_count` 为 4。
- 旧版配置仅给出 `core_count = 5` 时，自动拆分为 2 个高性能核和 3 个低功耗核，总核心数仍为 5。

### 4.2 与既有测试的关系

本轮同时运行了已有测试：

- `tests/test_imports.py`：确认 `tebs` 包以及新增的 `tebs.config` 模块可正常导入。
- `tests/test_models.py`：确认第1轮数据结构在加入配置模块后仍保持原有行为。

因此第2轮不仅验证了配置模块自身，也验证了它没有破坏第1轮已建立的数据结构和导入路径。

## 5. 与后续轮次衔接

当前配置模块已经可直接支撑：

- 第3轮 `task_factory.py`（读取场景与仿真时长配置）
- 第4轮 `environment.py`（读取 `delta_t_seconds`、日照/阴影周期、功率缺省）
- 第5轮 `energy_model.py`（读取 `E_min/E_max` 和能量单位约定）
- 第6轮 `thermal_model.py`（读取温度上限与热参数）
- 第12轮及以后 Gurobi 模块（读取 `time_limit`、`mip_gap`、线程参数）
