# 第22轮代码生成说明

## 1. 输入文档

- `模块化代码实现计划.md`
- `完整实验方案.md`
- `docs/第1轮代码生成说明.md` 至 `docs/第21轮代码生成说明.md`
- 当前已有代码：`tebs_scheduler`

## 2. 本轮目标

实现端到端小规模实验脚本，验证任务生成、环境生成、调度器、仿真器、指标和输出文件能够完整串联。脚本至少运行一个规则基线；当 Gurobi 可用时，可额外运行 `FCFS-Gurobi` 和 `RHC-MILP-Gurobi`。

## 3. 本轮生成结果

新增文件：

- `tebs_scheduler/scripts/run_small_experiment.py`
- `tebs_scheduler/tests/test_end_to_end_small.py`
- `docs/第22轮代码生成说明.md`

主要接口：

- `run_small_experiment(...)`：以函数形式运行小规模实验，便于测试和脚本复用。
- `main(...)`：命令行入口，支持 `--output-dir`、`--scenario-name`、`--random-seed`、`--num-tasks`、`--simulation-slots`、`--figures`、`--skip-gurobi`。

默认运行方法：

```text
safe_idle
fcfs_dvfs
tcn_scheduler
hybrid_gurobi_tcn
```

当 `include_gurobi=True` 且 `is_gurobi_available()` 为真时，额外加入：

```text
fcfs_gurobi
rhc_milp_gurobi
```

## 4. 测试与验收

### 4.1 测试命令

单轮验收命令：

```bash
cd tebs_scheduler
E:\study\bishe_code\.venv\Scripts\python.exe -m pytest tests/test_end_to_end_small.py
```

脚本手工运行命令示例：

```bash
cd tebs_scheduler
E:\study\bishe_code\.venv\Scripts\python.exe scripts/run_small_experiment.py --output-dir outputs/small_experiment --num-tasks 2 --simulation-slots 8 --skip-gurobi
```

本次实际执行的第18-22轮联合测试命令：

```bash
cd tebs_scheduler
E:\study\bishe_code\.venv\Scripts\python.exe -m pytest tests/test_dataset_builder.py tests/test_tcn_policy.py tests/test_constraint_filter.py tests/test_hybrid_scheduler.py tests/test_end_to_end_small.py
```

全量回归命令：

```bash
cd tebs_scheduler
E:\study\bishe_code\.venv\Scripts\python.exe -m pytest
```

### 4.2 测试覆盖文件

| 文件 | 角色 | 验收关注点 |
|---|---|---|
| `tebs_scheduler/scripts/run_small_experiment.py` | 第22轮生成脚本 | 小规模场景、方法列表、Gurobi 可用性分支、输出路径 |
| `tebs_scheduler/tests/test_end_to_end_small.py` | 第22轮测试代码 | 端到端运行、summary/solver summary、schedule/state 文件 |
| `tebs_scheduler/src/tebs/experiment_runner.py` | 第17轮 runner 扩展使用者 | 新增学习型方法能被 batch runner 调用 |

### 4.3 测试项、指标与预期结果

| 测试项 | 测试函数 | 测试指标 | 预期结果 | 测试结果 |
|---|---|---|---|---|
| 端到端小规模实验 | `test_run_small_experiment_writes_end_to_end_outputs` | run 数量、summary 文件、solver summary 文件、schedule/state 输出 | 不启用 Gurobi 时运行 4 个方法；所有 run 都写出 schedule/state；summary 文件存在 | 通过 |

### 4.4 测试项具体说明

1. `test_run_small_experiment_writes_end_to_end_outputs`

   调用 `run_small_experiment(...)`，设置 `num_tasks=2`、`simulation_slots=6`、`generate_figures=False`、`include_gurobi=False`。测试会完整经过任务生成、环境生成、四个调度器运行、仿真、指标计算和 CSV 写出。检查点包括：`summary.csv` 存在，`solver_summary.csv` 存在，方法集合包含 `safe_idle`、`fcfs_dvfs`、`tcn_scheduler`、`hybrid_gurobi_tcn`，且每个 run 都有 schedule 与 state 输出。

### 4.5 验收结论

第22轮测试结果：

```text
tests/test_end_to_end_small.py: 1 passed
```

第18-22轮联合测试结果：

```text
10 passed
```

全量回归结果：

```text
84 passed
```

结论：第22轮已完成端到端小规模实验脚本，小规模实验闭环能够稳定运行并写出指标与轨迹文件。
