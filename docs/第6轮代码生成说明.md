# 第6轮代码生成说明

## 1. 输入文档

- `模块化代码实现计划.md`
- `完整实验方案.md`
- 第1-5轮已有代码与说明文档

## 2. 本轮目标

按计划实现热模型：

- 生成 `tebs_scheduler/src/tebs/thermal_model.py`
- 生成 `tebs_scheduler/tests/test_thermal_model.py`
- 计算温度更新、计算热量、热预算和热约束违反。

## 3. 本轮生成结果

新增主要接口：

- `ThermalStepResult`：单时隙热状态核算结果。
- `update_temperature(...)`：按计算功耗、基础功耗、冷却项更新温度。
- `update_temperature_from_config(...)`：从配置读取热参数。
- `compute_thermal_budget_j(...)`：估算下一时隙仍可吸收的计算热量。
- `is_thermal_feasible(...)`：判断给定计算功耗是否会超过温度上限。

核心模型：

```text
Q_compute = P_com * delta_t_seconds
Q_base = P_base * delta_t_seconds
Q_cool = cooling_coeff * max(0, T_prev - T_ambient) * delta_t_seconds
T_next = T_prev + (Q_compute + Q_base - Q_cool) / C_th
thermal_violation = T_next > T_max
```

## 4. 测试与验收

新增 `tests/test_thermal_model.py`，覆盖：

- 高计算功耗连续执行时温度上升。
- 空闲且无基础功耗时温度可冷却。
- `T_next > T_max` 时 `thermal_violation=True`。
- `Q_compute = P_com * delta_t_seconds`。
- 热预算非负。

最终全量测试结果：

```text
42 passed
```

## 5. 后续衔接

第8轮仿真器在每个时隙调用 `update_temperature(...)`，第9轮指标模块统计 `thermal_violation_count` 与最高温度。
