# 热-电协同感知在轨任务调度完整实验方案

## 1. 实验目标

本实验用于支撑毕业论文中的代码实现与实验验证部分。实验对象为星载多核计算平台上的载荷处理任务调度问题，核心目标是验证热-电协同感知的滚动时域优化方法是否能够在能量约束、热约束和多核资源约束下，降低任务完成时延、拖期损失和约束违反次数。

实验重点包括：

1. 验证本文方法 `RHC-MILP-Gurobi` 在混合载荷任务集上的调度效果。
2. 与固定队列基线 `FCFS-Gurobi`、`FCFS-DVFS` 和 `FCFS-Intermittent` 进行对比。
3. 评估 Gurobi 在滚动时域求解中的速度、最优性和可扩展性。
4. 在大规模任务场景下，引入 TCN 学习型调度器，验证其作为快速近似策略的可行性。

## 2. 已确认的实现决策

### 2.1 关于基线是否全部使用 Gurobi

建议采用如下实现策略：

| 方法 | 是否完整使用 Gurobi | 建议 |
|---|---:|---|
| `RHC-MILP-Gurobi` | 是 | 作为本文主方法，必须完整使用 Gurobi 建模与求解。 |
| `FCFS-Gurobi` | 是 | 作为强基线，固定任务顺序，但用 Gurobi 优化核心分配和启动时刻。 |
| `FCFS-DVFS` | 不建议完整 Gurobi 化 | 建议采用规则仿真实现，必要时只将核心分配部分与 Gurobi 强基线对齐。 |
| `FCFS-Intermittent` | 不建议完整 Gurobi 化 | 建议采用规则仿真实现，重点体现暂停-恢复机制。 |

理由如下：

1. `DVFS` 和 `Intermittent` 本质上是对固定 FCFS 队列的设备级或系统级响应策略。PDF 中对二者的定义强调的是固定队列、频率档位切换、暂停恢复，而不是重新优化任务顺序。
2. 如果将 `FCFS-DVFS` 和 `FCFS-Intermittent` 全部 MILP 化，会引入大量额外变量，例如频率档位变量、任务进度变量、暂停状态变量、恢复状态变量。这会显著增加实现复杂度和求解时间，但并不能改变它们“不具备任务重排能力”的核心属性。
3. 论文对比的重点应是“本文任务级前瞻重排”相对于“固定队列 + DVFS / 间歇计算”的优势。如果基线也被建成复杂优化模型，容易弱化方法差异，并增加解释成本。
4. 为保证公平性，可以保留一个 `FCFS-Gurobi` 强基线：它固定任务顺序，但允许 Gurobi 优化核心分配和启动时间。这样既能体现异构多核资源分配能力，又不会让基线拥有任务重排能力。

因此，最终建议对比组设置为：

| 实验组 | 说明 |
|---|---|
| `RHC-MILP-Gurobi` | 本文方法，允许任务重排、任务-核心匹配和热-电前瞻约束。 |
| `FCFS-Gurobi` | 固定 FCFS 顺序，用 Gurobi 优化核心分配与启动时刻。 |
| `FCFS-DVFS` | 固定 FCFS 顺序，根据温度和电量阈值切换高/中/低频档。 |
| `FCFS-Intermittent` | 固定 FCFS 顺序，达到暂停阈值时停止计算，恢复后继续执行。 |
| `TCN-Scheduler` | 大规模场景下的学习型快速近似调度器。 |
| `Hybrid-Gurobi-TCN` | 小规模优先用 Gurobi，大规模或超时时用 TCN + 约束过滤。 |

### 2.2 关于默认实验参数

默认配置建议如下：

| 参数 | 默认值 | 依据与说明 |
|---|---:|---|
| 核心数量 | 4 核 | 采用 2 个高性能核 + 2 个低功耗核，能够体现异构多核上的任务-核心匹配。 |
| 时隙长度 `Delta t` | 60 s | PDF 中能量模型已暂定 1 min 为一个 slot；该粒度也便于遥感任务块级调度仿真。 |
| 预测窗口 `H` | 12 slots | 即 12 min。作为默认值可覆盖短期日照/阴影变化、热积累和任务队列变化，同时保持 MILP 规模可控。 |
| Gurobi `TimeLimit` | 5 s | 每个滚动窗口最多求解 5 s，适合离线实验与近实时分析。 |
| Gurobi `MIPGap` | 0.01 | 允许 1% gap，兼顾解质量和求解速度。 |
| 仿真总时长 | 120-240 slots | 覆盖多个任务释放和资源变化周期。 |
| 初始电量 | `0.7 E_max` | 避免初始即过低或过满导致实验失真。 |
| 最低安全电量 `E_min` | `0.2 E_max` | 对应最大放电深度约 80%。 |
| 初始温度 | 25 摄氏度 | 模拟安全工作温度起点。 |
| 温度上限 `T_max` | 75 摄氏度 | 用于仿真热安全上限，可后续根据平台参数调整。 |

关于 `H=12` 的说明：

1. 滚动时域优化和模型预测控制中的预测窗口长度通常需要在控制效果和计算复杂度之间折中。窗口过短会导致调度短视，窗口过长会使 MILP 变量和约束数量快速增长。
2. 本问题中每个时隙为 1 min，`H=12` 表示调度器能看到未来 12 min 的太阳能输入、基础功耗和热预算，足以观察短期资源变化并进行任务重排。
3. `H=12` 只作为默认配置，不作为最终唯一结论。实验中必须设置 `H` 的敏感性分析，建议测试 `H in {6, 8, 12, 16, 24}`，并报告求解质量和求解速度变化。
4. 若 `H=12` 下 Gurobi 最优解比例过低或超时比例过高，可将默认窗口降为 `H=8`；若求解速度充足且调度短视明显，可提高到 `H=16`。

### 2.3 关于大规模学习方法

确认优先实现 `TCN-Scheduler`。

理由如下：

1. 本问题的输入天然是固定预测窗口序列，例如未来 `H` 个时隙的太阳能输入、基础功耗、热预算、电量和温度变化。
2. TCN 能对固定长度时间窗口进行并行卷积计算，训练和推理通常比 LSTM 更快。
3. TCN 对长短期时间依赖具有稳定建模能力，适合用作 Gurobi 求解慢或超时时的快速近似策略。
4. 在论文实验中，TCN 可以先作为监督学习调度器，用 Gurobi 小规模/中规模最优轨迹生成标签；后续如果需要扩展，再加入强化学习对比。

## 3. 工程模块设计

推荐工程结构如下：

```text
tebs_scheduler/
  configs/
    default_sim.yaml
    scenarios.yaml
    gurobi.yaml
    tcn.yaml
  src/
    tebs/
      models.py
      task_factory.py
      environment.py
      energy_model.py
      thermal_model.py
      scheduler_base.py
      gurobi_adapter.py
      solver_monitor.py
      rhc_milp_gurobi.py
      baseline_fcfs_gurobi.py
      baseline_dvfs.py
      baseline_intermittent.py
      simulator.py
      metrics.py
      experiment_runner.py
      visualization.py
      learning/
        dataset_builder.py
        tcn_policy.py
        constraint_filter.py
        hybrid_scheduler.py
  tests/
    test_energy_model.py
    test_thermal_model.py
    test_task_factory.py
    test_rhc_constraints.py
    test_baselines.py
    test_solver_monitor.py
    test_simulator_integration.py
  outputs/
    schedules/
    metrics/
    figures/
    models/
```

## 4. 核心模块输入输出

| 模块 | 功能 | 输入 | 输出 |
|---|---|---|---|
| `models.py` | 定义任务、任务块、核心、系统状态和调度动作 | 任务参数、核心参数、系统参数 | `Task`、`TaskBlock`、`Core`、`SystemState`、`ScheduleDecision` |
| `task_factory.py` | 生成模拟任务集 | 场景名、随机种子、任务数量 | 7 类星上载荷任务集合 |
| `environment.py` | 生成轨道资源环境 | 仿真时长、日照/阴影周期、最大采集功率 | `P_harv(t)`、`P_base(t)`、环境热输入 |
| `energy_model.py` | 计算电池能量变化 | `E(t-1)`、`P_harv`、`P_base`、`P_com` | `E_load`、`E_harv`、`E(t)`、能量违约标记 |
| `thermal_model.py` | 计算温度与热预算 | 当前温度、计算功耗、基础热、环境热 | `T(t)`、`B(t)`、热违约标记 |
| `gurobi_adapter.py` | 封装 Gurobi 调用 | 变量、约束、目标函数、参数 | Gurobi 模型和求解结果 |
| `solver_monitor.py` | 记录求解速度和最优性 | Gurobi status、solve time、MIP gap | 最优比例、超时比例、求解时间统计 |
| `rhc_milp_gurobi.py` | 实现本文 RHC-MILP | 当前系统状态、任务队列、预测窗口 | 当前时隙调度动作、窗口计划 |
| `baseline_fcfs_gurobi.py` | 固定顺序强基线 | 固定 FCFS 队列、核心集合、环境预测 | 固定顺序下的优化调度结果 |
| `baseline_dvfs.py` | DVFS 基线 | FCFS 队列、频率档位、温度/电量阈值 | 每时隙频率档位和调度动作 |
| `baseline_intermittent.py` | 间歇计算基线 | FCFS 队列、暂停/恢复阈值 | 执行/暂停状态和调度动作 |
| `simulator.py` | 统一仿真主循环 | 调度器、任务集、环境、初始状态 | 调度轨迹、状态轨迹、任务完成记录 |
| `metrics.py` | 计算实验指标 | 调度轨迹、状态轨迹、任务记录 | 时延、拖期、吞吐、违约、求解统计 |
| `dataset_builder.py` | 生成 TCN 训练数据 | Gurobi 调度轨迹和系统状态 | 训练/验证/测试数据集 |
| `tcn_policy.py` | 学习型调度策略 | 状态序列和任务队列特征 | 当前时隙任务-核心分配建议 |
| `constraint_filter.py` | 过滤学习模型动作 | TCN 动作、系统约束 | 可行动作或安全回退动作 |
| `hybrid_scheduler.py` | 混合调度 | Gurobi 状态、TCN 预测、约束过滤器 | 当前时隙最终动作 |

## 5. 任务集设计

任务来源于论文材料中的星上载荷处理任务类型。

| ID | 任务类型 | 负载特征 | 块数建议 | 权重建议 |
|---|---|---|---:|---:|
| T1 | 云检测 | 中等 AI 推理 | 4-8 | 2 |
| T2 | 云去除与压缩 | 多阶段中等负载 | 4 | 2 |
| T3 | SAR 船舶检测 | 高功耗、高产热、高时效 | 6-10 | 5 |
| T4 | 光学目标检测 | 中高 AI 推理 | 4-8 | 3 |
| T5 | 变化检测 | 高功耗、高时效、多阶段 | 4-6 | 5 |
| T6 | 图像压缩 | 低到中等负载 | 3-6 | 1 |
| T7 | SAR 后端处理 | 高功耗、多阶段 | 6-10 | 4 |

每个任务块包含：

```text
task_id
block_id
release_time r_j
deadline d_j
weight w_j
duration_by_core l_j,m,c
power_by_core p_j,m,c
predecessor block
```

异构核心参数遵循：

```text
高性能核：执行时间短，功耗高
低功耗核：执行时间长，功耗低
```

## 6. 场景设计

| 场景 | 任务规模 | 任务构成 | 目的 |
|---|---:|---|---|
| S1 轻负载 | 20-40 个任务 | 云检测、图像压缩、少量目标检测 | 验证资源宽松时本文方法不会引入明显额外开销。 |
| S2 混合负载 | 40-80 个任务 | 7 类任务混合 | 主实验，验证任务重排、热-电协同和核心匹配收益。 |
| S3 高压力 | 60-120 个任务 | SAR、变化检测、SAR 后端比例提高 | 验证热约束和能量约束紧张时的违约抑制能力。 |
| S4 阴影区压力 | 40-100 个任务 | 任务集中在阴影区前后释放 | 验证太阳能输入变化下的前瞻调度能力。 |
| S5 大规模 | 100、200、500 个任务 | 混合任务 | 验证 Gurobi 扩展性，并引入 TCN。 |
| S6 消融场景 | 40-80 个任务 | 混合任务 | 去掉部分约束或目标项，验证各设计贡献。 |

## 7. 调度方法设计

### 7.1 本文方法：RHC-MILP-Gurobi

每个滚动时刻 `t0`：

1. 观测 `E(t0-1)`、`T(t0-1)` 和当前任务队列。
2. 生成预测窗口 `H(t0) = {t0, ..., t0 + H - 1}`。
3. 预测窗口内的 `P_harv(t)`、`P_base(t)` 和热预算 `B(t)`。
4. 建立 Gurobi MILP 模型。
5. 求解后只执行当前时隙 `t0` 的调度动作。
6. 更新任务队列、电量和温度。
7. 滚动到下一时隙。

目标函数：

```text
min alpha * sum_j w_j(C_j - r_j)
  + beta  * sum_j w_j L_j
  + mu    * sum_j w_j R_j(t0 + H)
```

主要约束：

```text
启动-运行关系
任务块最多启动一次
释放时间约束
任务内部顺序约束
多核容量约束
非抢占执行约束
完成时间约束
拖期变量约束
能量状态更新约束
电池容量约束
热预算约束
```

### 7.2 FCFS-Gurobi

该基线固定全局任务队列顺序：

```text
pi = sort(J, r_j)
```

Gurobi 只优化：

```text
任务块启动时间
任务块核心分配
多核容量
能量和热状态
```

但不允许：

```text
跳过队首任务
改变任务释放顺序
根据热-电状态主动选择后续任务
```

### 7.3 FCFS-DVFS

任务顺序固定为 FCFS。每个核心有高、中、低三个频率档位。

频率规则：

```text
if T(t) >= T_crit or E(t) <= E_crit:
    q(t) = L
elif T_warn <= T(t) < T_crit or E_crit < E(t) <= E_warn:
    q(t) = M
else:
    q(t) = H
```

任务进度按频率比例推进：

```text
progress += f_c,q / f_c,H
```

DVFS 功耗按频率和电压缩放：

```text
P_dvfs = P_high * (V_q / V_H)^2 * (f_q / f_H)
```

### 7.4 FCFS-Intermittent

任务顺序固定为 FCFS。

暂停规则：

```text
if T(t) >= T_pause or E(t) <= E_pause:
    pause all compute cores
```

恢复规则：

```text
if T(t) <= T_resume and E(t) >= E_resume:
    resume previous tasks
```

暂停时：

```text
P_com(t) = 0
仅保留 P_base(t)
当前任务进度不增加
```

### 7.5 TCN-Scheduler

TCN 作为大规模任务下的快速近似方法。

输入特征：

```text
E(t), T(t)
核心空闲状态
任务剩余块数
任务权重
deadline slack = d_j - t
任务块功耗 p_j,m,c
任务块执行长度 l_j,m,c
未来 H 个时隙 P_harv
未来 H 个时隙 P_base
未来 H 个时隙热预算 B(t)
```

输出：

```text
每个核心选择一个任务块或 idle
```

训练标签：

```text
由 RHC-MILP-Gurobi 在小规模和中规模任务集上的最优或近优调度轨迹生成。
```

安全机制：

TCN 输出必须经过 `constraint_filter`，过滤条件包括：

```text
释放时间合法
前驱任务块已完成
核心容量合法
非抢占执行合法
当前能量不低于 E_min
预测热预算不超限
```

## 8. 能量与热模型

### 8.1 能量模型

内部统一使用焦耳 `J`。

```text
E_load(t) = (P_base(t) + P_com(t)) * Delta t
E_harv(t) = P_harv(t) * Delta t
E(t) = min(max(E(t-1) + E_harv(t) - E_load(t), E_min), E_max)
```

能量不可行判据：

```text
E_deficit(t) > E_avail(t)
```

其中：

```text
E_deficit(t) = max(0, E_load(t) - E_harv(t))
E_avail(t) = max(0, E(t-1) - E_min)
```

图表展示时：

```text
E_Wh = E_J / 3600
```

### 8.2 热模型

计算功耗近似转化为计算产热：

```text
Q_compute(t) = P_com(t) * Delta t
```

温度可用一阶热模型更新：

```text
T(t+1) = T(t) + heat_gain(t) / C_th - heat_loss(t)
```

滚动时域热预算约束：

```text
Q_cum(tau) <= B(tau), for all tau in H(t0)
```

## 9. 评价指标

### 9.1 调度性能指标

```text
平均完成时间
平均响应时间 C_j - r_j
加权响应时间 sum_j w_j(C_j - r_j)
总拖期惩罚 sum_j w_j max(0, C_j - d_j)
完成任务数
吞吐量
核心利用率
```

### 9.2 约束安全指标

```text
热约束违反次数
能量约束违反次数
最高温度
最低电量
平均电量
暂停次数
```

### 9.3 Gurobi 求解指标

```text
optimal_slot_ratio = 最优解时隙数 / 总求解时隙数
feasible_slot_ratio = 有可行解时隙数 / 总求解时隙数
timeout_ratio = 超时时隙数 / 总求解时隙数
infeasible_ratio = 不可行时隙数 / 总求解时隙数
avg_solve_time
p50_solve_time
p95_solve_time
avg_mip_gap
p95_mip_gap
avg_node_count
avg_objective_value
```

### 9.4 TCN 指标

```text
单步推理时间
动作可行率
约束过滤后动作保留率
相对 Gurobi 目标值差距
相对 RHC-MILP 的完成时间差距
热约束违反次数
能量约束违反次数
大规模任务下的扩展性
```

## 10. 实验流程

1. 固定随机种子生成任务集和轨道环境。
2. 对同一任务集分别运行所有调度方法。
3. 每个滚动时隙记录系统状态、调度动作、功耗、电量、温度和 Gurobi 状态。
4. 仿真结束后计算所有指标。
5. 每个场景至少运行 10 个随机种子。
6. 输出均值、标准差和改进比例。
7. 对主要方法进行显著性分析。
8. 生成表格和图。

## 11. 参数敏感性实验

### 11.1 预测窗口 H

测试：

```text
H in {6, 8, 12, 16, 24}
```

观察：

```text
调度性能是否提升
热/能违约次数是否下降
Gurobi 平均求解时间是否上升
最优解时隙比例是否下降
MIP gap 是否增大
```

### 11.2 Gurobi TimeLimit

测试：

```text
TimeLimit in {1s, 3s, 5s, 10s}
```

观察：

```text
最优解比例
可行解比例
超时比例
平均目标函数值
调度性能
```

### 11.3 MIPGap

测试：

```text
MIPGap in {0, 0.01, 0.03, 0.05}
```

观察：

```text
求解时间变化
目标函数变化
任务完成时间变化
约束违约变化
```

### 11.4 任务规模

测试：

```text
N in {20, 50, 100, 200, 500}
```

观察：

```text
Gurobi 扩展性
最优解比例下降趋势
TCN 推理速度
Hybrid 方法稳定性
```

## 12. 消融实验

| 消融项 | 目的 |
|---|---|
| 去掉热约束 | 验证热预算约束对温度安全的贡献。 |
| 去掉能量约束 | 验证电池安全约束贡献。 |
| 去掉剩余工作量惩罚 | 验证滚动窗口是否出现短视行为。 |
| 固定核心分配 | 验证异构核心匹配贡献。 |
| 不允许任务重排 | 退化为 FCFS，验证任务重排贡献。 |
| 不使用预测窗口 | 只看当前时隙，验证前瞻性贡献。 |

## 13. 输出文件设计

```text
outputs/
  schedules/
    rhc_milp_gurobi_schedule.csv
    fcfs_gurobi_schedule.csv
    fcfs_dvfs_schedule.csv
    fcfs_intermittent_schedule.csv
    tcn_schedule.csv
    hybrid_schedule.csv
  metrics/
    summary.csv
    solver_summary.csv
    solver_trace.csv
    scenario_comparison.csv
    sensitivity_h.csv
    sensitivity_timelimit.csv
    sensitivity_mipgap.csv
  figures/
    gantt_rhc_milp_gurobi.png
    energy_temperature_comparison.png
    objective_comparison.png
    tardiness_comparison.png
    solver_time_distribution.png
    scale_vs_solve_time.png
    scale_vs_optimal_ratio.png
    tcn_vs_gurobi_large_scale.png
  models/
    tcn_policy.pt
```

## 14. 图表设计

必须生成：

1. 调度甘特图：展示不同方法任务执行顺序和核心分配。
2. 电量曲线：单位展示为 Wh，原始数据保存为 J。
3. 温度曲线：展示温度随时间变化。
4. 功耗曲线：展示计算功耗和基础功耗。
5. 任务完成时间箱线图：比较不同方法的响应时间分布。
6. 指标柱状图：比较加权响应时间、拖期惩罚、吞吐量。
7. Gurobi 求解时间分布图：展示平均、P95、最大求解时间。
8. 任务规模-求解时间曲线：展示 Gurobi 可扩展性。
9. 任务规模-最优解比例曲线：展示大规模下最优求解比例下降。
10. TCN 与 Gurobi 大规模对比图：展示速度和质量折中。

## 15. 预期结论

预期论文可围绕以下结论展开：

1. 在混合负载和高压力场景中，`RHC-MILP-Gurobi` 相比固定队列基线能够降低平均响应时间和拖期惩罚。
2. `RHC-MILP-Gurobi` 能够减少热约束和能量约束违反次数，尤其在阴影区和高功耗任务密集释放时更明显。
3. `FCFS-DVFS` 能降低瞬时功耗，但会拉长高功耗任务执行时间，导致后续任务等待增加。
4. `FCFS-Intermittent` 能规避部分约束风险，但暂停次数增加，吞吐量下降。
5. 随任务规模和预测窗口长度增加，Gurobi 最优解比例下降，平均求解时间和 MIP gap 上升。
6. `TCN-Scheduler` 在大规模任务下能够显著降低单步决策时间，但必须结合约束过滤保证安全。
7. `Hybrid-Gurobi-TCN` 能够在小规模保持较高解质量，在大规模保持较好实时性，是后续工程化扩展方向。

## 16. 实施优先级

建议按以下顺序实现：

1. 实现任务、核心、环境、能量和热模型。
2. 实现统一仿真器和指标计算。
3. 实现 `RHC-MILP-Gurobi`。
4. 实现 `solver_monitor.py`，记录 Gurobi 求解速度和最优性。
5. 实现 `FCFS-Gurobi` 强基线。
6. 实现 `FCFS-DVFS` 和 `FCFS-Intermittent` 规则基线。
7. 完成轻负载、混合负载和高压力场景实验。
8. 完成 `H`、`TimeLimit`、`MIPGap`、任务规模敏感性实验。
9. 用 Gurobi 轨迹生成 TCN 训练数据。
10. 实现 `TCN-Scheduler` 和 `Hybrid-Gurobi-TCN`。
11. 完成大规模实验和最终图表。

## 17. 参考依据

1. 本地论文材料：`docs/问题建模.pdf`、`docs/求解方案设计.pdf`、`docs/对比方案设计.pdf`、`docs/实验任务选型与任务集构建.pdf`。
2. Gurobi 官方状态码文档：`OPTIMAL`、`TIME_LIMIT`、`INFEASIBLE`、`SUBOPTIMAL` 等状态可用于统计最优解比例、超时比例和可行解比例。链接：https://docs.gurobi.com/projects/optimizer/en/current/reference/numericcodes/statuscodes.html
3. Gurobi 参数文档与帮助中心：`TimeLimit` 可限制单次求解时间，`MIPGap` 可控制返回解的相对最优性 gap。链接：https://support.gurobi.com/hc/en-us/articles/14126085438481-How-do-I-set-parameters-in-Gurobi ，https://support.gurobi.com/hc/en-us/articles/8265539575953-What-is-the-MIPGap
4. 模型预测控制与滚动时域优化相关文献通常指出，预测窗口长度需要在性能提升和计算复杂度之间折中，并应通过敏感性实验确定。参考：MathWorks MPC horizon 说明，https://www.mathworks.com/help/mpc/ug/choosing-sample-time-and-horizons.html ；Brudigam 等关于非均匀预测窗口的 MPC 研究，https://arxiv.org/abs/2108.08014
5. TCN 相关序列建模研究表明，卷积式时序模型可作为 RNN/LSTM 的有效替代，适合固定窗口序列输入和快速并行推理。参考：Bai, Kolter, Koltun, "An Empirical Evaluation of Generic Convolutional and Recurrent Networks for Sequence Modeling"，https://arxiv.org/abs/1803.01271
